"""Ranking and position sizing.

Implemented in later milestones. See repo task list.

Planned:
    scoring.py      — composite edge score (edge, sharpe, kelly, fees, decay, corr)
    kelly.py        — fractional-Kelly for binary markets with bid/ask + fees
    portfolio.py    — correlation tracking + diminishing-marginal-value penalty
    fees.py         — Kalshi fee schedule (currently "kalshi_default_v1")
"""
